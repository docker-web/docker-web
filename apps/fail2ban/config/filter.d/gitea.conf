[Definition]
# Filtre pour Gitea - détecte les tentatives d'authentification échouées

failregex = ^.*Failed authentication attempt for .* from <HOST>.*$
            ^.*Authentication failed for .* from <HOST>.*$
            ^.*Invalid credentials from <HOST>.*$
            ^.*Login failed, attempted from <HOST>.*$

ignoreregex =
