#!/bin/bash

source src/env.sh

YEAR=$(echo $DOCKERWEB_VERSION | cut -d'.' -f1)
MONTH=$(echo $DOCKERWEB_VERSION | cut -d'.' -f2)
VERSION=$(echo $DOCKERWEB_VERSION | cut -d'.' -f3)

((VERSION++))

sed -i "s|DOCKERWEB_VERSION=.*|DOCKERWEB_VERSION=\"$NEW_VERSION\"|g" src/env.sh
git add src/env.sh
